discord-dnd-bot {{ .Chart.AppVersion }} installed as release "{{ .Release.Name }}" in namespace "{{ .Release.Namespace }}".

Components deployed:
{{- if .Values.gateway.enabled }}
  • gateway — {{ if .Values.gateway.autoscaling.enabled }}autoscaled {{ .Values.gateway.autoscaling.minReplicas }}–{{ .Values.gateway.autoscaling.maxReplicas }}{{ else }}{{ .Values.gateway.replicaCount }} replica(s){{ end }}
{{- end }}
{{- if .Values.worker.enabled }}
  • worker  — {{ if .Values.worker.autoscaling.enabled }}autoscaled {{ .Values.worker.autoscaling.minReplicas }}–{{ .Values.worker.autoscaling.maxReplicas }}{{ else }}{{ .Values.worker.replicaCount }} replica(s){{ end }}
{{- end }}

1. Verify the pods are healthy:

   kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

2. Tail the gateway logs (it registers slash commands on startup):

   kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/component=gateway -f

{{- if not .Values.secrets.existingSecret }}
{{- if not .Values.secrets.discordToken }}

⚠  No Discord token was provided. Set secrets.discordToken (or point
   secrets.existingSecret at a Secret containing DISCORD_TOKEN) and upgrade:

   helm upgrade {{ .Release.Name }} discord-dnd-bot/discord-dnd-bot \
       --set secrets.discordToken=YOUR_BOT_TOKEN
{{- end }}
{{- end }}

3. Scale the worker pool to process more jobs in parallel:

   kubectl scale deploy/{{ include "discord-dnd-bot.fullname" . }}-worker -n {{ .Release.Namespace }} --replicas=5

The gateway and worker scale independently — that's the point. Enjoy the campaign!
